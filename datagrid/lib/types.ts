export interface SalesData {
  id: number;
  name: string;
  department: string;
  value: number;
}

export type User = {
  userId: string;
  name: string;
  email: string;
  profileUrl: string;
  organizationId: string;
};
